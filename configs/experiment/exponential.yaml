# @package _global_

seed: 12345

defaults:
  - override /model: nfvae_exponential.yaml
  - override /datamodule: exponential.yaml

logger:
  wandb:
    name: exponential-nfvae
    job_type: train

trainer:
  gpus: 0
  max_epochs: 1000
  resume_from_checkpoint: False
